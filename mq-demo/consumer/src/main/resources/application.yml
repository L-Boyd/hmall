logging:
  pattern:
    dateformat: MM-dd HH:mm:ss:SSS
spring:
  rabbitmq:
    host: 192.168.1.7
    port: 5672
    virtual-host: /hmall  # 虚拟主机
    username: hmall
    password: ${rabbitmq.password}
